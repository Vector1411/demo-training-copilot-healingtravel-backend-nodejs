# 02_coding_standards

- TypeScript strict, ESM imports với `.js` extension
- Throw `AppError(httpStatus,errorCode,message,details?)` cho lỗi dự kiến
- TODO phải trỏ về docs liên quan (để Copilot đọc đúng ngữ cảnh)

